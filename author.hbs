{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}

{{! The big featured header }}

{{! Everything inside the #author tags pulls data from the author }}
{{#author}}
    <header class="main-header author-head {{#if @site.cover_image}}" style="background-image: url({{@site.cover_image}}){{else}}no-cover{{/if}}">
        <nav class="main-nav overlay clearfix">
            <a class="back-button iconextra-right-arrow" href="{{@site.url}}">الرئيسية</a>
            <a class="subscribe-button icon-feed" href="{{url}}rss/">{{name}}</a>
        </nav>
    </header>

    <section class="author-profile inner">
        {{#if feature_image}}
        <figure class="author-image">
            <div class="img" style="background-image: url({{img_url}})"><span class="hidden">صور {{name}}</span></div>
        </figure>
        {{/if}}
        <h1 class="author-title">{{name}}</h1>
        <h2 class="author-bio">{{bio}}</h2>
        <div class="author-meta">
            <span class="author-stats"><i class="icon-stats"></i> {{plural ../pagination.total empty='لا توجد تدوينات' singular='% تدوينة' plural='% تدوينات'}}</span>
            <br/>
            {{#if location}}<span class="author-location icon-location">{{location}}</span>{{/if}}
            <br/>
            {{#if website}}<span class="author-link icon-link"><a href="{{website}}">{{website}}</a></span>{{/if}}
        </div>
    </section>
{{/author}}

{{! The main content area on the homepage }}
<main class="content" role="main">

    {{! The tag below includes the post loop - partials/loop.hbs }}
    {{> "loop"}}

</main>